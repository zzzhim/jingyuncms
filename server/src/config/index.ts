export const SECRET_KEY = "admin-secret-zzzhim"

export const SESSION_KEY = "admin-session-zzzhim"

export const port = 4000

export const localUrl = process.env.APP_NODE_ENV === "dev" ? `http://localhost:${port}` : ''